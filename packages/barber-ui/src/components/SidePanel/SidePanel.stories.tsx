import { Meta, StoryObj } from "@storybook/react";
import { SidePanel, SidePanelProps } from "./";

export default {
  title: "Components/SidePanel",
  component: SidePanel,
} as Meta<SidePanelProps>;

export const Default: StoryObj<SidePanelProps> = {};
